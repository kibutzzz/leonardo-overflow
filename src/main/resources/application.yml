spring:
  application:
    name: LeonardoOverflow

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB103Dialect
        format_sql: true
  datasource:
    url: jdbc:mariadb://localhost:3306/leonardo_overflow?serverTimezone=UTC&useLegacyDatetimeCode=false
    username: 'root'
    password: 'toor'
    driver-class-name: org.mariadb.jdbc.Driver

#jwt
jwt:
  secret: securityOopsie
  expiration: 86400000

